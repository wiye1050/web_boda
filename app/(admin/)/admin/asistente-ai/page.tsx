import { AIConfigView } from "@/components/admin/AIConfigView";

export const metadata = {
  title: "Entrenar Asistente AI | Alba & Guille",
};

export default function AIConfigPage() {
  return <AIConfigView />;
}
